// var logger = require('../middlewares/logger');
var logger = require('../middlewares/Logger');
var opts = {
    method: "file",
    level: "info",
    debug: true,
}
//logger.debug();
// var a = logger.log(opts,new Date());
// console.log(a());
// app.use(logger('dev'))
// var log = new logger();
// log.debug(new Date());
// logger.debug(new Date());
try{
    casdsak;
}catch (e){
    logger.log(opts,e);
}

